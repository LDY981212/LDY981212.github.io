/**
 * 히어로 바로 아래에 세우는 핵심 성과.
 *
 * 여기 적는 수치는 전부 프로젝트 모달의 트러블슈팅에 근거가 있는 것만 쓴다.
 * 카드를 누르면 그 근거가 있는 모달이 열리므로, 요약과 원본이 어긋나면
 * 바로 들킨다. 인상만 주는 형용사("최적화", "대폭 개선")는 넣지 않는다.
 */
export interface ImpactItem {
  /** 카드에서 가장 크게 읽히는 값. 단위를 포함한 짧은 문자열. */
  metric: string;
  label: string;
  body: string;
  project: string;
  /** 근거가 있는 프로젝트 모달의 router. */
  router: string;
}

const impactItems: ImpactItem[] = [
  {
    metric: "25건",
    label: "동시 처리 한계",
    body: "동시 작업이 열몇 건을 넘으면 서버가 죽었습니다. 모니터링에는 디스크 대기만 급등한 것으로 찍혀 증설을 검토하던 참이었는데, 부하 스크립트와 자원 측정기를 직접 만들어 재보니 디스크는 사용률이 20%도 되지 않았고 실은 메모리가 부족해 스왑이 일어나고 있었습니다. 증설안을 접고 단계마다 다른 상한을 건 세마포어로 바꾸자 25건을 전부 실패 없이 처리했고 스왑도 사라졌습니다.",
    project: "ClipNow",
    router: "clipnow",
  },
  {
    metric: "매초 → 0",
    label: "시청 화면 전체 재렌더",
    body: "남은 시간을 매초 상태에 반영하다 보니 영상과 채팅을 포함한 화면 전체가 1초마다 다시 그려지고 있었습니다. 매초 바뀌어야 하는 값과 화면이 실제로 달라져야 하는 시점은 다르다고 보고, 표시용 시간은 컨트롤 바가 떠 있는 동안에만 갱신하도록 하고 상태에는 시청 모드가 바뀔 때만 변하는 값 하나만 남겼습니다.",
    project: "Player",
    router: "player",
  },
  {
    metric: "0.113 → 0",
    label: "이 페이지의 CLS",
    body: "첫 화면 강조어가 2.6초마다 바뀌면서 뒷 문장을 조금씩 밀어내고 있었습니다. 한 번의 움직임이 작아 눈으로는 넘어갔는데, 실제 Chrome에 PerformanceObserver를 붙여 재보니 4분을 머무는 동안 92번 밀려 CLS가 0.113까지 올라갔습니다. 회전을 걷어내고 로컬에서 0을 확인했지만 배포한 주소로 다시 재보니 2MB짜리 폰트가 늦게 교체되며 남는 시프트가 또 있어서, 폰트를 177KB로 줄이고 폴백 지표까지 맞춰 라이브에서도 0으로 만들었습니다.",
    project: "개인 포트폴리오 웹",
    router: "individual",
  },
];

export default impactItems;
